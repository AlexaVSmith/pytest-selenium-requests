[project]
name = "sporty"
version = "0.1.0"
description = ""
authors = [
    {name = "Alexa Smith",email = "alexa.ballantine@gmail.com"}
]
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "pytest-selenium (>=4.1.0,<5.0.0)",
    "pytest (>=8.3.5,<9.0.0)",
    "requests (>=2.32.3,<3.0.0)",
    "pylint (>=3.3.7,<4.0.0)",
    "black (>=25.1.0,<26.0.0)",
]

[tool.poetry]
packages = [{include = "sporty", from = "src"}]


[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
